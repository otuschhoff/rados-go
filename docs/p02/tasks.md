# P02 Task and Exit-Gate Status

P02 is not complete. Implementation and oracle reproduction are complete, but
the full quality/fuzz qualification and accountable human review remain open.

## Task Matrix

| Task | Acceptance | Status |
| --- | --- | --- |
| Banner and revision | Exact revision-1 bytes, fragmented reads, malformed and unsupported input | Implemented; tests passed |
| CRC framing | One-to-four bounded segments, CRC32C, alignment, late completion/abort, corruption tests | Implemented; tests passed |
| Secure framing | Direction-separated AES-128-GCM records, padding, tag failure, counter exhaustion, deterministic test secrets only | Implemented; tests passed |
| Control and messages | Bounded typed control payloads and message segment/length consistency | Implemented; tests passed |
| Session ownership | Single owner, bounded queues/bytes/inflight work, pumps, transaction dispatch, keepalive | Implemented; synthetic tests passed |
| ACK/replay/reset | ACK is not completion; reconnect identity, duplicate/gap handling, partial/full reset | Implemented; synthetic tests passed |
| Cancellation/shutdown | Pending cleanup, connector context cancellation, transport close interruption, no shared socket deadline mutation | Implemented; synthetic tests passed |
| Source-derived fixture evidence | Five strict manifests, verifier, exact parity tests, independent oracle | Verifier, parity, and pinned reproduction passed |
| Upstream `FrameAssembler` evidence | Seven strict manifests and vectors generated by pinned Ceph `FrameAssembler` | Reproduction and byte parity passed |
| Live authentication/connectivity | Real CephX secure monitor connection | Out of P02; required in P03 |

## Exit-Gate Matrix

| Gate | Command/evidence | Current status |
| --- | --- | --- |
| Formatting and package tests | `make unit-p02`; repository formatting check in `make verify-p02` | Package tests passed locally |
| Race tests | `go test -race ./...` through `make quality-p02` | Passed locally |
| Fixture parity and verifier | `make differential-p02` | Passed locally |
| Verifier executable | `go run ./tools/p02-verify` | Passed locally |
| cgo-disabled build, vet, module, cross-builds | `make verify-p02` | Passed locally |
| Independent local oracle compile | Apple clang 21 with OpenSSL 3.6.4 | Passed locally |
| Pinned fixture reproduction | `make reproduce-p02` | Independent and upstream generators passed |
| Upstream-executed oracle | `make reproduce-p02-upstream`; pinned Ceph `FrameAssembler` vectors | Seven vectors passed; four overlap vectors match the independent oracle byte-for-byte |
| Six fuzz targets, 60 seconds each | `GOMAXPROCS=1 make fuzz-p02` | Passed locally; session fuzzer retained a queued-replay regression seed |
| Six nightly fuzz targets, five minutes each | `make fuzz-p02-nightly` in scheduled CI | Pending CI execution |
| Live authenticated Ceph connection | P03 real-monitor integration | Not claimed |
| Security/license review | Named reviewer, reviewed tree, findings, disposition, redistribution decision | **Pending human review** |

P02 must remain incomplete until the remaining full gates pass and the human
review record is complete. Synthetic peer coverage does not close the P03
authentication/connectivity gate; that evidence is maintained separately in
P03.